name: Lint Awesome List

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run awesome-lint
        uses: awesome-lint/awesome-lint@v0
